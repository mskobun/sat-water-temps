"""Common utilities shared across ECOSTRESS, Landsat, and backfill processors.

Import from submodules directly, e.g.:
    from common.dates import to_sort_date
    from common.storage import upload_to_r2
"""
